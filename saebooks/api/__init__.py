"""saebooks JSON API surface.

Version-namespaced — every router lives under
``/api/v{n}/...`` so breaking changes can ship behind a new version
without disturbing existing clients. Phase 0 is ``v1`` only.
"""
