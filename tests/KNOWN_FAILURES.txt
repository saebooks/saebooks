SAE Books — known test-suite failures (CI triage tracker)
=========================================================

The CI "tests" job (.github/workflows/test.yml) runs the FULL pytest suite
via the isolated docker-compose stack (scripts/run-tests.sh).

STATUS (2026-06-05): the suite is GREEN and this job is now BLOCKING
(continue-on-error removed). Verified by the full suite plus 3 consecutive
fresh-DB reruns of the previously-flaky journal/projects/tpar/void clusters,
all exit 0 — the historic "journal cluster 32<->44 wobble" no longer
reproduces. The postgres first-init TCP race that could throw a false red is
closed at the source (db healthcheck probes -h 127.0.0.1) and the CI step
retries once as belt-and-suspenders. If a real red appears, fix it or list it
here with a deselect/xfail; only re-add continue-on-error to un-block in an
emergency.

Historic note (pre-2026-06-05): the job was INFORMATIONAL because the suite
was not yet green —

The BLOCKING gate is lint (ruff) + typecheck (mypy --strict on the core
domain-model/config layer) + migrations (alembic upgrade head on a fresh
DB). Those three must be green to merge.

This file tracks the path to a green, BLOCKING test job. The end state is
to flip the tests job to blocking (remove continue-on-error) once the suite
is green or once every remaining red is listed here with a reason and a
tracked deselect/xfail.

Harness deselects (already excluded in docker-compose.test.yml):
  - tests/services/lodgement/test_adapter_registry.py
  - tests/test_m0_synthetic_nz_company.py

Known-red baseline (full-suite run on origin/main + this branch):
  See the latest CI "tests" job log for the live FAILED/ERROR node ids.
  These are pre-existing reds (journal ordering flake, projects/tpar/
  void-archive clusters per the build-rebuild notes), NOT regressions
  introduced by the lint/type cleanup in this branch. Triaging them to
  green is the follow-up that unblocks flipping this job to BLOCKING.
